package com.teavm.react.core;

import org.teavm.jso.JSObject;

/**
 * Represents anything that can be rendered by React: elements, strings, numbers, arrays, null.
 */
public interface ReactNode extends JSObject {
}
