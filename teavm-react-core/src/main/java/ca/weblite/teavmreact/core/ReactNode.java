package ca.weblite.teavmreact.core;

import org.teavm.jso.JSObject;

/**
 * Represents anything React can render: elements, strings, numbers, arrays, null.
 */
public interface ReactNode extends JSObject {
}
